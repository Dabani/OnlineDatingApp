<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    
    <a class="navbar-brand" href="/">
      <img src="./images/rambagiza.png" alt="RAMBAGIZA"  height="24">
    </a>
    {{#if newSmile}}
      <a href="/showSmile/{{newSmile._id}}" class="mx-2"><i class="bi bi-emoji-smile smile" data-bs-toggle="tooltip" data-bs-placement="bottom" title="New Smile"></i></a>
    {{else}}
      <a class="navbar-brand" href="#">{{title}}</a>
    {{/if}}
    {{#if unread}}
      <a href="/chats"><i class="bi bi-envelope letter" data-bs-toggle="tooltip" data-bs-placement="bottom" title="New Message"></i></a>
    {{/if}}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        {{#if user}}
          <li class="nav-item">
            <a class="nav-link" href="/singles"><i class="bi-people"></i> Singles</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="/profile"><i class="bi-person-lines-fill"></i> Profile</a>
          </li>

          <li class="nav-item">
            <a class="nav-link" href="/logout"><i class="bi-box-arrow-right"></i> Logout</a>
          </li>
        {{else}}
          <li class="nav-item">
            <a class="nav-link" href="/about"><i class="bi-card-image"></i> About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/contact"><i class="bi-card-heading"></i>  Contact</a>
          </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>